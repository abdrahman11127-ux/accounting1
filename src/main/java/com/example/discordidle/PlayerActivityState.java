package com.example.discordidle;

/**
 * Normalised activity states shared between the RuneLite plugin and the
 * standalone monitor application.
 */
public enum PlayerActivityState
{
    ACTIVE,
    IDLE,
    LOGGED_OUT
}
